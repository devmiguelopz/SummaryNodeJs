const Logger = require("./Logger");
const logger = new Logger();
const loggerSave = (response, message) => response && console.info(message)
logger.CallEvent(loggerSave)
logger.CallEvent(loggerSave)
logger.CallEvent(loggerSave)
logger.CallEvent(loggerSave)
